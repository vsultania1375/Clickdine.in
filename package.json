{
  "name": "clickdine-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "^6.4.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.43.0",
    "wrangler": "^4.107.0"
  }
}
